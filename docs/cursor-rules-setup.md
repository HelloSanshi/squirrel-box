# Cursor Rules 初始化规范

## 📋 规则说明

在项目初始化阶段，需要在项目根目录下创建 `.cursor/rules/` 文件夹，并按照以下规范创建相应的规则文件。这些规则文件将指导 AI 助手在开发过程中遵循项目约定。

---

## 🚀 初始化步骤

### 1. 创建规则目录

在项目根目录下创建 `.cursor/rules/` 文件夹：

```bash
mkdir -p .cursor/rules
```

### 2. 创建规则文件

在 `.cursor/rules/` 目录下创建以下 4 个规则文件：

---

## 📝 规则文件内容

### 文件 1: `.cursor/rules/changlogs.mdc`

```markdown
---
alwaysApply: true
---
每次进行代码更新之后 在 CHANGELOG.MD 记录跟新日志
```

**用途**：确保每次代码更新都会在 `CHANGELOG.MD` 中记录变更日志，保持项目变更历史清晰可追溯。

---

### 文件 2: `.cursor/rules/git-commit.mdc`

```markdown
---
alwaysApply: true
---
所有影响仓库进度的改动（代码 / 文档 / 配置）完成后，需在当前工作会话内及时执行一次 `git commit`，并在提交信息中清晰描述主要变更。若已按 @changlogs.mdc 更新了 `CHANGELOG.MD`，请确保同一提交一并包含该文件，避免改动堆积或信息脱节。
```

**用途**：规范 Git 提交流程，确保变更及时提交且 commit message 清晰，同时保证 CHANGELOG 与代码变更同步提交。

---

### 文件 3: `.cursor/rules/progress.mdc`

```markdown
---
alwaysApply: true
---
本规范与 @changlogs.mdc 配套，要求 Cursor 在交付任何影响项目进度的改动时同步更新以下内容：

1. `README.md` 中的「路线图」章节必须真实反映项目各阶段的完成度，并列出当前仍待完成的重点事项（测试、扩展功能、稳定性等）。若交付会调整阶段内容，需在同一提交中更新该表。
2. 项目主计划文档（通常位于 `docs/` 目录下，如 `docs/YYYYMMDD-项目名-L.md`）是进度单一事实来源。实现新能力或完成阶段任务时，要勾选对应清单、更新时间戳，并将新的 backlog / 风险写入「当前待办」或风险段落。
3. 额外的大功能文档用于记录具体交付细节；当其 TODO 有状态变化时，要同步回主计划文档与 README，避免多处信息不一致。
4. 如新增任务或未完成事项，请在上述文档中创建/更新待办条目，并在提交中记录到 `CHANGELOG.MD`（遵循 @changlogs.mdc）。
```

**用途**：确保项目进度文档（README 路线图、任务文档等）与实际开发进度保持同步，避免文档与代码脱节。

**注意**：这个规则是通用模板，需要根据你的实际项目结构调整：
- 路线图的章节名称（如「🎯 路线图」）
- 项目阶段的命名方式（如 Phase 1-4、Sprint 1-N 等）
- 主计划文档的具体路径和命名规范

---

### 文件 4: `.cursor/rules/script-docstring.mdc`

```markdown
---
description: Python脚本文件需要包含完整的使用文档
globs: scripts/*.py
alwaysApply: false
---

# Python 脚本文档规范

所有位于 `scripts/` 目录下的 Python 脚本必须在文件开头包含完整的文档字符串（docstring），格式如下：

\```python
#!/usr/bin/env python3
"""
脚本简短描述（一行）

详细描述:
说明脚本的功能、数据源、处理逻辑等

使用方式:
---------
# 基本用法
python scripts/xxx.py [必要参数]

# 带可选参数的用法
python scripts/xxx.py --option value

# 其他常见用法示例
python scripts/xxx.py --start 1 --end 10

参数说明:
---------
--param1    参数1说明，默认值
--param2    参数2说明，默认值
...

输出格式:
---------
描述输出文件的格式、字段含义等

示例输出:
---------
给出1-2行实际输出样例
"""
\```

## 必须包含的部分

1. **shebang 行**: `#!/usr/bin/env python3`
2. **简短描述**: 一句话说明脚本用途
3. **使用方式**: 至少2-3个命令行示例
4. **参数说明**: 列出所有命令行参数及其默认值
5. **输出格式**: 描述脚本的输出（文件格式、字段含义）
6. **示例输出**: 给出实际输出样例

## 好的示例

参考项目中已有脚本的文档格式。

## 原因

- 让其他开发者快速理解如何使用脚本
- 避免需要阅读代码才能知道参数
- 方便复制粘贴命令直接运行
```

**用途**：规范 Python 脚本的文档格式，确保所有脚本都有清晰的使用说明，降低使用门槛。

**注意**：这个规则通过 `globs: scripts/*.py` 指定只对 `scripts/` 目录下的 Python 文件生效。

---

## 🔧 规则文件说明

### MDC 文件格式

`.mdc` (Markdown with Configuration) 文件是 Cursor 的规则文件格式，包含两部分：

1. **YAML Front Matter**（文件开头的 `---` 包裹部分）
   - `alwaysApply: true` - 规则始终生效
   - `alwaysApply: false` - 规则按需生效（如仅对特定文件类型）
   - `description` - 规则描述
   - `globs` - 规则适用的文件路径模式

2. **Markdown 内容**（规则的具体说明）

### 规则之间的协同

这 4 个规则文件相互配合，形成完整的开发规范：

```
changlogs.mdc ──┐
                ├──> 确保变更可追溯
git-commit.mdc ─┘

progress.mdc ────> 确保文档同步更新

script-docstring.mdc ──> 确保脚本可用性
```

---

## ✅ 验证规则生效

创建文件后，可以通过以下方式验证规则是否生效：

1. **重启 Cursor**：重新加载规则配置
2. **测试规则**：
   - 修改一些代码，看 AI 是否提示更新 `CHANGELOG.MD`
   - 完成改动后，看 AI 是否提示执行 `git commit`
   - 创建 Python 脚本，看 AI 是否要求添加完整的 docstring

---

## 📦 快速初始化脚本

如果需要在多个项目中使用这些规则，可以创建一个初始化脚本：

```bash
#!/bin/bash
# scripts/init-cursor-rules.sh

# 创建规则目录
mkdir -p .cursor/rules

# 创建 changlogs.mdc
cat > .cursor/rules/changlogs.mdc << 'EOF'
---
alwaysApply: true
---
每次进行代码更新之后 在 CHANGELOG.MD 记录跟新日志
EOF

# 创建 git-commit.mdc
cat > .cursor/rules/git-commit.mdc << 'EOF'
---
alwaysApply: true
---
所有影响仓库进度的改动（代码 / 文档 / 配置）完成后，需在当前工作会话内及时执行一次 `git commit`，并在提交信息中清晰描述主要变更。若已按 @changlogs.mdc 更新了 `CHANGELOG.MD`，请确保同一提交一并包含该文件，避免改动堆积或信息脱节。
EOF

# 创建 progress.mdc
cat > .cursor/rules/progress.mdc << 'EOF'
---
alwaysApply: true
---
本规范与 @changlogs.mdc 配套，要求 Cursor 在交付任何影响项目进度的改动时同步更新以下内容：

1. `README.md` 中的「路线图」章节必须真实反映项目各阶段的完成度，并列出当前仍待完成的重点事项（测试、扩展功能、稳定性等）。若交付会调整阶段内容，需在同一提交中更新该表。
2. 项目主计划文档（通常位于 `docs/` 目录下，如 `docs/YYYYMMDD-项目名-L.md`）是进度单一事实来源。实现新能力或完成阶段任务时，要勾选对应清单、更新时间戳，并将新的 backlog / 风险写入「当前待办」或风险段落。
3. 额外的大功能文档用于记录具体交付细节；当其 TODO 有状态变化时，要同步回主计划文档与 README，避免多处信息不一致。
4. 如新增任务或未完成事项，请在上述文档中创建/更新待办条目，并在提交中记录到 `CHANGELOG.MD`（遵循 @changlogs.mdc）。
EOF

# 创建 script-docstring.mdc
cat > .cursor/rules/script-docstring.mdc << 'EOF'
---
description: Python脚本文件需要包含完整的使用文档
globs: scripts/*.py
alwaysApply: false
---

# Python 脚本文档规范

所有位于 `scripts/` 目录下的 Python 脚本必须在文件开头包含完整的文档字符串（docstring），格式如下：

```python
#!/usr/bin/env python3
"""
脚本简短描述（一行）

详细描述:
说明脚本的功能、数据源、处理逻辑等

使用方式:
---------
# 基本用法
python scripts/xxx.py [必要参数]

# 带可选参数的用法
python scripts/xxx.py --option value

# 其他常见用法示例
python scripts/xxx.py --start 1 --end 10

参数说明:
---------
--param1    参数1说明，默认值
--param2    参数2说明，默认值
...

输出格式:
---------
描述输出文件的格式、字段含义等

示例输出:
---------
给出1-2行实际输出样例
"""
```

## 必须包含的部分

1. **shebang 行**: `#!/usr/bin/env python3`
2. **简短描述**: 一句话说明脚本用途
3. **使用方式**: 至少2-3个命令行示例
4. **参数说明**: 列出所有命令行参数及其默认值
5. **输出格式**: 描述脚本的输出（文件格式、字段含义）
6. **示例输出**: 给出实际输出样例

## 好的示例

参考项目中已有脚本的文档格式。

## 原因

- 让其他开发者快速理解如何使用脚本
- 避免需要阅读代码才能知道参数
- 方便复制粘贴命令直接运行
EOF

echo "✅ Cursor rules 初始化完成！"
echo "📁 规则文件已创建在 .cursor/rules/ 目录下"
echo "🔄 请重启 Cursor 以加载规则"
```

使用方式：

```bash
chmod +x scripts/init-cursor-rules.sh
./scripts/init-cursor-rules.sh
```

---

## 🎯 最佳实践

1. **项目初始化时立即创建**：在项目创建后第一时间设置规则
2. **团队共享**：将 `.cursor/rules/` 目录提交到 Git，确保团队成员使用相同规则
3. **定期审查**：根据项目需求定期审查和调整规则内容
4. **文档同步**：确保规则文档与实际项目结构保持同步（如 `progress.mdc` 中的文档路径）

---

## 📚 相关资源

- [Cursor Rules 官方文档](https://docs.cursor.com/context/rules)
- [MDC 文件格式说明](https://docs.cursor.com/context/rules#mdc-format)

---

**最后更新**: 2025-11-28

